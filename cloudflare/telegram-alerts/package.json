{
  "name": "telegram-alerts",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker que reenvía anomalías de Cloudflare (Notifications + polling propio) a Telegram.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  }
}
